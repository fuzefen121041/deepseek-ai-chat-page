# DeepSeek AI Chat Frontend

一个炫酷的 AI 聊天界面，基于 React + Vite 构建。

## ✨ 功能特性

- 🎨 **炫酷的 UI 设计**
  - 渐变色背景动画
  - 流畅的过渡效果
  - 响应式设计
  - 毛玻璃效果（Glassmorphism）

- 💬 **聊天功能**
  - 实时消息显示
  - 对话历史管理
  - 打字加载动画
  - 消息复制功能

- ⚡ **性能优化**
  - 基于 Vite 的快速构建
  - Framer Motion 动画
  - 自适应文本框
  - Token 使用统计

## 🚀 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 配置环境变量

复制 `.env.example` 为 `.env`：

```bash
cp .env.example .env
```

编辑 `.env` 文件：

```env
# 本地开发
VITE_API_URL=http://localhost:3000

# 生产环境（部署后替换为你的后端地址）
# VITE_API_URL=https://your-backend.vercel.app
```

### 3. 启动开发服务器

```bash
npm run dev
```

浏览器将自动打开 `http://localhost:5173`

## 📦 构建生产版本

```bash
npm run build
```

构建产物将输出到 `dist` 目录。

## 🎨 UI 特性说明

### 动态背景
- 三个渐变色球体的浮动动画
- 毛玻璃效果的容器
- 流畅的颜色过渡

### 消息动画
- 滑入动画效果
- 打字指示器
- 复制按钮交互
- Token 使用显示

### 输入框
- 自适应高度
- Enter 发送消息
- Shift+Enter 换行
- 发送按钮动画

## 🛠 技术栈

- **React 18** - UI 框架
- **Vite** - 构建工具
- **Framer Motion** - 动画库
- **Axios** - HTTP 客户端
- **CSS3** - 样式和动画

## 📱 响应式设计

界面完全适配：
- 💻 桌面端（>768px）
- 📱 移动端（<768px）
- 🖥 大屏幕（>1200px）

## 🎯 项目结构

```
react-frontend/
├── src/
│   ├── components/
│   │   ├── ChatMessage.jsx      # 消息组件
│   │   ├── ChatMessage.css
│   │   ├── ChatInput.jsx        # 输入框组件
│   │   └── ChatInput.css
│   ├── App.jsx                  # 主应用组件
│   ├── App.css                  # 主应用样式
│   ├── main.jsx                 # 入口文件
│   └── index.css                # 全局样式
├── index.html
├── vite.config.js
└── package.json
```

## 🔧 自定义配置

### 修改主题颜色

编辑 `src/index.css` 中的 CSS 变量：

```css
:root {
  --primary-color: #6366f1;      /* 主色调 */
  --secondary-color: #8b5cf6;    /* 次要色 */
  --accent-color: #ec4899;       /* 强调色 */
  /* ... 更多变量 */
}
```

### 调整动画速度

在各个 `.css` 文件中修改 `animation-duration` 和 `transition` 属性。

## 📝 使用提示

1. 首次使用建议点击欢迎页面的快捷问题
2. 支持多轮对话，会自动保持上下文
3. 可以随时点击"清空对话"开始新会话
4. 消息支持复制到剪贴板
5. 显示每次请求的 Token 使用量

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！
